# Personal-machine-only packages.
brew "gh"                    # github cli (also the git credential helper in .gitconfig)
cask "maccy" if OS.mac?      # clipboard history — keep off work machines (persists secrets)
